# MongoDB Connection URI
MONGO_URI=mongodb+srv://your_username:your_password@your_cluster.mongodb.net/foodmine?retryWrites=true&w=majority

# Server Port
PORT=5000

# JSON Web Token Secret
JWT_SECRET=your_jwt_secret_key

# Allowed Origins for CORS (comma-separated values)
ALLOWED_ORIGINS=http://localhost:3000,https://your-frontend-domain.com

# Node Environment (production or development)
NODE_ENV=production 